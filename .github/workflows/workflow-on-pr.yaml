name: Deploy to Agile On PR Merge
# 123444
on:
    push:
      branches:  
         - main

jobs:
    test-job:
        runs-on: ubuntu-latest
        steps:
            - name: Run a test command
              run: echo "This is a test job!"                  
